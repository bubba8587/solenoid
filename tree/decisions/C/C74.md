---
id: C74
name: semanticZoomRawScale
title: "The far-zoom switch reads the raw CSS scale at 0.3, never folded with the pixel ratio"
status: active
parents: ["[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [semanticZoomRawScale]
---

## Decision

The far-zoom simplification (the class `html.solenoid-semantic-zoom`, with plain CSS doing the swap) switches on the raw CSS scale, at a threshold of 0.3 (`SEMANTIC_ZOOM_SCALE`). The surface being zoomed owns the toggle: `syncSemanticZoomFor` runs from that surface's viewport drivers.

## Why

Folding the device pixel ratio into the scale was tried and measured to be worse. It made the switch trip at a different apparent zoom on each display, and legibility depends on apparent size. At 0.3 a card draws at about 30% of its size (a 200px card becomes about 60px), where its body text is unreadable but the card is still a clear block. The threshold is conservative, applying only to a far overview, but a user can actually reach it.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the leaf and `dte show` derives "implemented by" (A8 threeLayers upstream).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "the far-zoom gate reads the raw CSS scale at 0.3, never dpr-folded" by Claude.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
