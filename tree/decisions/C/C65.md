---
id: C65
name: domOrderStacking
title: "Node stacking is each node's React Flow zIndex, set by nodeZIndex"
status: active
parents: ["[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [domOrderStacking]
---

## Decision

React Flow stacks nodes by their `zIndex`, and the area-plane ladder is that data, not DOM order: standoff bars lowest (drawn by `StandoffLayer`), groups at −2 and conduits at −1 (stamped by `nodeZIndex` in `flowModel.ts`), ordinary nodes at 0. The fixed lifts (isolate endpoints, an open group picker, the selected cable) stay explicit z-index writes.

## Why

A group's body has to sit below its members, or it catches their pointer events; React Flow's own model for that is `zIndex`, so the ladder lives there. It is the rete-era ladder under a new mechanism. The rete surface stacked by DOM order, with `simpleNodesOrder` re-appending the picked card, and that ended with the rete surface ([[B10]] reactFlowView). One side effect went with it: selecting a node no longer moves its DOM element, so the old constraint that such a move closes a native popup (settled by a probe) no longer applies. The `pointerdown` swallow on form controls survives only because it prevents a drag.

## Consequences

- The standoff layer's depth is its own CSS (`StandoffLayer.css`, `z-index: -3`), since it renders in React Flow's viewport portal rather than as a node.

- **Reopen if:** cards visibly stack wrongly after selection, or a control needs the last-selected card on top, which React Flow's `zIndex` model does not give.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the leaf and `dte show` derives "implemented by" (A8 threeLayers upstream).
- 2026-09-22 Bare rule names linked as `[[ID]]` wikilinks by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged. Corrected against the code: `nodeZIndex` stamps groups and conduits only, and the −3 standoff bars are drawn by `StandoffLayer`.
- 2026-09-22 title changed from "node stacking is per-node `zIndex` (`nodeZIndex`, flowModel.ts)" by Claude.
- 2026-09-22 Corrected by Claude: the standoff layer's −3 is not set anywhere; the rule stays, the gap is labeled.
- 2026-09-22 The standoff layer now sets its −3 (it had none, so bars painted over cards moved onto them); the Unenforced label is gone. By Claude.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
