---
id: C48
name: appendLadder
title: "One append node per shape, taking any number of inputs of any kind"
status: active
parents: ["[[B11]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [appendLadder]
authorized_by: the author
---

## Decision

There is one append node per shape, never one per kind of value, and each takes any number of inputs holding any kind of value: Concat Lists for lists, the Stack node (VSTACK and HSTACK) for matrices, where an uneven edge pads with `#N/A` cells as in Excel, and Append for Frames, where columns match by name and a missing column fills with blanks. Each grows a new input row as you connect one.

## Why

[[B11]] maximalMerge: one append per shape rather than a node per kind of value. Uneven matrix stacking pads the way Excel does ([[A5]] excelParity).

## Consequences

- **Deliberately not merged, so no one merges them later:** Interleave, Pad, Repeat, Add Column, and Build Frame versus Frame from Lists. "Add one row" is Get Row followed by Append; a plain list connected to Append, which matches columns by name, is refused as a trap.
- **Reopen if:** a single input that accepts several cables exists; it would replace the growing input rows.

## History

- 2026-09-22 Bare rule names linked as `[[ID]]` wikilinks by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged. Corrected: the 2-D append is the Stack node, whose ops are VSTACK and HSTACK.
- 2026-09-22 title changed from "ONE N-ary element-agnostic append per rank" by Claude.
- 2026-09-24 Rewritten for a human reader at the author's ask; the rule is unchanged ("wire-only extensible rows" and "variadic socket" now read in plain words). By Claude Opus 5.5.
- 2026-09-24 title changed from "One append node per rank, taking any number of inputs of any element type" by Claude Opus 5.5, authorized by the author.
