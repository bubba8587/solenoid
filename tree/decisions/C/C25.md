---
id: C25
name: firstClassUnits
title: "a value's unit is first-class; only the algebra or Convert changes it"
status: active
parents: ["[[B17]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [firstClassUnits]
---

## Decision

A unit is part of a value: 5 m and 5 km are different values, so relabeling a unit is overwriting the number. A Format Controller downstream of a value with a unit locks and mirrors it; only Convert, or a calculation that breaks the tag, changes a unit. Join keys with units compare by dimension and base-SI magnitude, so `5 km` equals `5000 m`, and a currency's code is its identity, so $5 ≠ 5€.

## Why

[[A6]] names first-class units as a headline divergence from Excel. A unit that a dropdown could relabel would make every number with a unit untrustworthy.

## Consequences

- A free-text custom unit is a dimension axis of its own, never dimensionless: collapsed to dimensionless, a custom unit divided by seconds read as Hz. Two custom names that differ only in case are the same axis.
- A Convert downstream fills an FC's unit only when the FC has none. An authored unit stands, and a real clash surfaces as the Convert's error rather than a silent rewrite.
- **Reopen if:** nothing. This is value-model integrity, in the same class as [[C24]] arraySemantics.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the leaf and `dte show` derives "implemented by" (A8 threeLayers upstream).
- 2026-09-18 Re-parented by Claude Fable 5.1 from [[A6]] to [[B17]] typedValueModel: the B ring gained the family's parent (author's ask: flesh out ring B).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-23 Added the custom-axis and Convert-dictation consequences by Claude (lifted from src/graph/dimension.ts and src/graph/nodes/formatController.ts).
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
