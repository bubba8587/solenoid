---
id: C36
name: captureBeforeSwap
title: "A canvas-swapping verb captures first and guards the rebuild"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [captureBeforeSwap]
---

## Decision

**MUST:** every `documentStore` verb that changes which document is on screen calls `captureCurrent()` before switching and guards on `isGraphRebuilding()`. A capture that cannot serialize returns false and the verb stays on the document, since switching would drop its unsaved canvas. Without the capture, edits made within the autosave delay are discarded; without the guard, the verb can race a load and serialize a half-built canvas into the current document. Three exceptions carry their reason:
- `restore` runs at startup, when nothing is live yet;
- `remove` skips the capture because the document is going away, and capturing would resurrect its dying edits;
- `reloadCurrent` needs no guard of its own, because the guard lives inside `captureCurrent`; its own gate is the reveal lock.

## Why

Both failures happened: a verb raced a load and saved a half-built canvas, and the fallback after a refused document discarded outgoing edits ([[B12]] losslessSaves). Both are pinned in `sourceInvariants.test.ts`.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-24 Failed-capture clause added by Claude Opus 5.5: `captureCurrent` returns false when serialize throws and every swap verb stays put (duplicate also captured only when copying the current document; fixed).
