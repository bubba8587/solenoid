---
id: C55
name: aiWholeDocRewrite
title: "AI edits are a whole-document text-form rewrite, gated and diffed"
status: active
parents: ["[[B13]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [aiWholeDocRewrite]
---

## Decision

An AI edit is a whole-document rewrite of the text form: the model emits a full replacement, the strict validator gates the apply, and the approval step shows the diff from old to new. There is no edit-operation layer.

## Why

[[B13]] aiInScope requires an AI edit to take the same validated, approved path as a human's, and the text form already is that path. An edit-op layer would be a second grammar and a second validator, with partly applied states to handle, and at the size of document a model works on it would be pure cost.

## Consequences

- Nothing in the AI service touches the document: a validated rewrite reaches only the palette's approval diff. The rewrite is canonicalized through the text-form writer first, so the diff shows only semantic change, and each repair round feeds the validator's issues back to the model. The demo key swaps only the transport, so validation, repair and canonicalization run for real in the demo too.
- **Reopen if:** documents outgrow reliable whole-document regeneration, or the MCP port ships (its typed tools would be the op layer).

## History

- 2026-09-22 Bare rule names linked as `[[ID]]` wikilinks by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-23 Cage mechanics summarized by Claude (lifted from src/graph/aiService.ts, aiDemo.ts).
