---
id: C57
name: commentMinimalism
title: Knowledge lives in leaves, specs, tests and commits; a comment is the last resort
status: active
parents:
  - "[[B9]]"
supersedes: []
superseded_by:
conflicts_with: []
date: 2026-09-15
ratified_by:
confidence: high
aliases:
  - commentMinimalism
authorized_by: the author
---

## Decision

When a comment is reviewed, the default outcome is deletion. A comment survives only if it states a constraint on a specific line that is invisible in the code, types, tests, specs and commits. Revision history goes to commit messages, rulings to decision leaves, and investigations to `docs/dev-notes.md`. Test files are exempt for now.

## Why

A comment is a copy of knowledge that has a better home, and copies drift ([[B9]] rulesWithoutMemory). A reason that governs code belongs in a leaf the code cites ([[B8]] treeIsTheHome), where `trace` and `blast` find it; in a comment it is found only by someone who happens to open that file.

## Consequences

- **Where knowledge lives, strongest home first.** (1) The code itself, its names and types: rename before commenting. (2) A machine check: a "do not do X" that a test can enforce becomes a test, because a comment guard is a plea and a test is a wall. (3) The decision leaf or the spec, found through the routing table in `docs/README.md`. (4) The commit message, which holds all revision history. (5) A comment, only for a line-level constraint that is invisible at every level above and likely to be broken by someone editing that exact line. Both conditions must hold.
- **What gets cut.** Revision history ("the old code…", "previously…") moves to the commit message; a present-tense constraint tangled up with it survives on its own. A dated ruling in a comment is a misfiled decision: make it a leaf, then delete the comment. Investigations, measurements and negative results have one home (dev-notes or the spec). A routed file carries no prose beyond its `[[ID]]` line, and content that exists only in a comment is promoted before it is cut. A module header states scope and hard constraints only, cites the spec rather than quoting it, and runs at most two lines, with no scope prose at all on a routed file. A doc comment carries what the signature cannot (ordering, what null means, units, side effects); a JSDoc that restates the name goes. Never translate code into English. TODOs go to `docs/backlog.md`, and commented-out code is deleted.
- **The blast-radius test** (comment, or leaf and spec): a constraint earns a comment only when a mistake could happen at a single edit site, no doc lies on the path to that mistake, and no stronger home is available. Prose that governs a module's architecture goes the other way and is promoted to a leaf or spec.
- **Compression.** A surviving comment is one sentence stating the constraint (enumerated contracts and truth tables excepted). Section banners appear only in files of about 400 lines or more. No Excel-equivalent or duplicate annotations that the catalog or a spec already carries. Test files (`*.test.ts` and `*.test.tsx`) are exempt for now.
- A file's `[[ID]]` citation line is the machine-checked link to its governing leaves, and the one line a routed file must carry. A rationale comment moves into its leaf's Why and shrinks to that citation. DTE's own rule B38 commentsMigrate makes the same split: the why goes to the node, the how to the spec, and only what the code does stays in the code.
- **Reopen if:** regressions keep happening that a comment would have prevented. Fix the routing first; a comment is the last resort.

## History
- 2026-09-18 docs/code-comments.md folded into Consequences and deleted by Claude Fable 5.1 (author: every system-describing doc is a leaf or a spec).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "knowledge lives in nodes, specs, tests and commits; a comment is the last resort" by Claude.
- 2026-09-24 title changed from "Knowledge lives in nodes, specs, tests and commits; a comment is the last resort" by Claude Opus 5.5, authorized by the author.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
- 2026-09-24 Quality pass toward ratification: "node" reads "leaf" where it means a tree item. By Claude Opus 5.5.
