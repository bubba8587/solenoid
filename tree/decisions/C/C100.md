---
id: C100
name: chartIsAValue
title: "A chart is a value: the chart socket carries a flat, JSON-safe self-describing figure"
status: active
parents: ["[[B17]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
confidence: high
aliases: [chartIsAValue]
---

## Decision

A chart is a value. The `chart` socket carries a self-describing figure (its kind, series, options and structured payload) that is flat and JSON-safe, so it rides a cable, saves with the document, and draws anywhere a value draws: a Display, a Report embed, a popup.

## Why

If the figure were the node's own drawing, nothing downstream could show, embed or format it, and a Report couldn't carry a chart the way it carries a number. A flowing figure is what makes the Format Controller's `chart` family ([[C94]] formatFamilyGates), Report embeds and a chart built from another node's output (Gantt from Schedule) work without special wiring.

## Consequences

- The figure picks its renderer, not the surface: recharts for interactive plots (one lazy chunk, [[C97]] rechartsLazyChunk), plain CSS or SVG cards for structured payloads (KPI, Gauge, the Record views), and a supersampled 2-D canvas for data-heavy figures, which stays one DOM element however many points it draws and takes its theme colors from the live CSS variables when it draws.
- The payload is data, never geometry. The Gantt value, for example, carries rows, links and non-working spans as serials plus the resolved view settings, and the layout produces pixels at the measured width, because the same figure draws at four sizes (chip, Display, popup, Report).
- Options ride the figure as text ([[C96]] chartOptionsAreMatplotlib).
- **Reopen if:** figures need to write edits back into their data. They would then need an identity, not just a value ([[C71]] noBarEditing).

## History

- 2026-09-18 Consequence added by Claude Fable 5.1: the payload is data, not geometry (lifted from packages/gantt-layout).
- 2026-09-18 Re-parented by Claude Fable 5.1 from [[A6]] to [[B17]] typedValueModel: the B ring gained the family's parent (author's ask: flesh out ring B).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
