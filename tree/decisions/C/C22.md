---
id: C22
name: rowFormulaRefs
title: "In a computed column, a bare name is the whole column and `@name` is this row's cell"
status: active
parents: ["[[B16]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [rowFormulaRefs]
---

## Decision

**MUST:** in a computed column's formula, a bare name or `[Name]` is the whole column as a list, and `@name`, `@[Name]` or `[@Name]` is this row's cell, the same as Excel's table references. A whole column where one value is needed is a `#SHAPE!` on that row pointing at `@`, never a silent read of this row's cell.

## Why

[[B16]] oneFormulaSurface measures against current Excel, and this is how Excel's table references read. Any other rule fails with a believable number instead of an error: if bare names read this row's cell, `revenue / SUM(revenue)` gives 1.0 on every row, because `SUM(revenue)` sums a single cell, and nothing looks wrong. Formulas that mix both readings, like `SUMIFS(amt, cat, @cat)`, can't be written at all if one spelling has to mean both.

## Consequences

- When names collide, a column wins, then the built-in names `row` and `rows`, then a LAMBDA's captured values, then an input wired into the node.
- A LAMBDA's parameters are bound to the row. Any other name in its body, bare or `@`, becomes a captured input.
- Brackets spell names that have spaces or other characters a plain name can't hold: `[Unit Price]`, `@[Unit Price]`.
- How it's built: [[computed-columns]].
- **Reopen if:** nothing. This is the Excel model the surface exists to mirror.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-18 Re-parented by Claude Fable 5.1 from [[A5]] to [[B16]] oneFormulaSurface: the B ring gained the family's parent (author's ask: flesh out ring B).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
- 2026-09-24 Rewritten for a human reader at the author's ask; the rule is unchanged. The file pointer (`computedColumnCore.ts`) and the resolution order stand in [[computed-columns]]. By Claude Opus 5.5.
- 2026-09-24 Quality pass toward ratification: the Why argues from B16 and states the counter-example as a present consequence; a code name left the Decision. By Claude Opus 5.5.
