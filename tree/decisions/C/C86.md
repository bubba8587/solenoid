---
id: C86
name: membershipByGesture
title: "Group membership changes only on an explicit gesture; a collapsed group never gains a member"
status: active
parents: ["[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
confidence: high
aliases: [membershipByGesture]
---

## Decision

Group membership changes only on an explicit gesture: dragging a node in or out, the Group action on a selection, or a manual box resize over a node. Autofit wraps the box around the existing members and never re-derives membership. A collapsed group never gains a member. Membership is exclusive and stable: a node belongs to at most one group and keeps it while its center is inside, so an overlapping group cannot steal it.

## Why

When membership was worked out from where boxes sat after the app resized a group, it silently pulled in bystanders whose center the shrunk box happened to cover. A collapsed group is drawn small with its members hidden, so a node dropped or created over it joined and then vanished the next time the group collapsed. Both are the same failure: a membership change the user did not ask for.

## Consequences

- A Format Controller docked to a node belongs to whatever group its node belongs to; it never joins or leaves on its own.
- How each gesture changes membership is in the group-expand-push spec (§ Who is a member).
- **Reopen if:** an explicit "absorb everything under the box" action is wanted (then it is its own gesture, not a side effect).

## History

- 2026-09-22 British spellings changed to American by Claude; no rule changed.
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-24 Rewritten for a human reader at the author's ask; the rule is unchanged. By Claude Opus 5.5.
