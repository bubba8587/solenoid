---
id: C33
name: saveBindsMain
title: "Persistence binds the main graph, never the active surface"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [saveBindsMain]
---

## Decision

**MUST:** the composite drill-in substitutes the editor, area and history through the `activeGraph.ts` seam for canvas operations only. `getEditor()`, serialization, autosave and load always resolve the main graph, so a save taken while drilled in serializes the document, not the open subgraph.

## Why

Getting this wrong is total, silent data loss ([[B12]] losslessSaves). An autosave during a drill-in would write the composite's internal subgraph over the document, and the file would still be valid, so nothing would flag it.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Duplicate *Why:* label removed by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "Persistence binds MAIN, never the active surface" by Claude.
