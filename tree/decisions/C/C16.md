---
id: C16
name: polarsEngine
title: "the relational engine is native Polars on desktop and a JS oracle on web, not DuckDB"
status: active
parents: ["[[B2]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [polarsEngine]
---

## Decision

The relational verbs run on native Polars on desktop and on the JS oracle (a pure JavaScript implementation that gives the same answers) on the web, behind the `FrameBackend` seam. DuckDB is not used, and no second engine is added "for web" without reopening this decision.

## Why

[[B2]] webTryDesktopFull makes the web look-and-try and desktop the full product, so the full engine belongs on desktop, where native Polars runs, and the web needs only an oracle that gives identical answers.

## Consequences

- Standing risks: Polars API churn (pinned at 0.46), softened by the seam, by the JS oracle as the reference, and by the cargo parity tests ([[D29]] oneVerbCorpus). Keeping web and desktop in parity is a permanent cost; budget for it.
- **Reopen if:** a production Polars-WASM path appears, or the web becomes a full target. DuckDB (one engine for both targets) is the scoped alternative, and the seam makes that swap one module.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the node and `dte show` derives "implemented by" (A8 threeLayers upstream).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
