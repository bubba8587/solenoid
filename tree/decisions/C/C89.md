---
id: C89
name: standoffsSolveLast
title: "Standoffs are axis-band constraints; the solver runs last and a cluster is one rigid block"
status: active
parents: ["[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
confidence: high
aliases: [standoffsSolveLast]
contested_by: Claude Opus 5.5
---

## Decision

A standoff is a constraint the user declared, so no layout pass may leave it violated. The pure solver runs last, after every pass that affects layout (the drag chain-pull, expand push, collapse, autofit and Tidy). For the same reason, a cluster joined by standoffs is one rigid block to every layout operation: Tidy lays it out as a single super-node, and a push moves every member by the cluster's largest displacement.

## Why

A constraint that a layout pass can silently break is not a constraint, and running the solver last is what gives "declared" its meaning. Treating the cluster as a block is the same idea from the other side: a Tidy that pulled a pair apart and then let the solver snap it back would visibly fight itself.

## Consequences

- The band semantics, the anchors and the 45° lock are in [[standoffs]].
- The no-overlap pass ([[C112]] noOverlapsEver) runs after the solver and moves each cluster as one unit, so it never breaks a band.
- A live drag applies the bands only (never `forceLock`), so an unlocked slant survives a drag. Every layout operation forces the lock for its single solve without touching the saved flag.
- Seeds follow the author's rule: groups liberally, standoffs sparingly. A seed standoff only pins an unwired explanatory Note to what it explains, and a data node never has a standoff to its consumer.
- The known gaps of the first version (dormant endpoints inside collapsed groups, no undo entries) are listed in [[standoffs]].
- **Reopen if:** standoffs should become ELK constraints, or need to bind group members and conduits.

## Contest

### Contest 2026-09-24 by Claude Opus 5.5: keep wins

Opposite (bands as soft hints any pass may break) makes a declared constraint meaningless; deletion leaves nothing saying clusters move rigidly. Keep. The no-overlap pass that now follows the solver moves each cluster as one unit, so it never breaks a band and the rule holds.

## History

- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 Spec paths became wikilinks when decisions/ and specs/ moved into the tree/ vault; by Claude. No wording changed.
- 2026-09-24 contested by Claude Opus 5.5; keep won.
- 2026-09-24 Consequence added: the no-overlap pass follows the solver and moves clusters whole, by Claude Opus 5.5.
