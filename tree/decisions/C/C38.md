---
id: C38
name: sinkRunButtonOnly
title: "A node that writes outside the app acts only when its Run button is pressed, and opens disarmed"
status: active
parents: ["[[B18]]", "[[B1]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [sinkRunButtonOnly]
authorized_by: the author
---

## Decision

**MUST:** a node that changes something outside the app for good (a write to disk or to a vault) acts only when the user presses its Run button, or on an explicit command-line run, and only while it is armed; never when the graph recalculates. Until then it shows a preview. Arming is never saved, so a node always arrives disarmed, whether from opening a file, pasting or a restored placeholder.

## Why

[[B18]] safeToShare says opening a document never acts outside the app, so an irreversible write has to wait for a deliberate press. Acting on recalculation would write to disk on any edit, and an arm switch that was saved would let merely opening a shared file write to the reader's disk. Writing back into the vault ([[B1]] obsidianBet) is the main such write.

## Consequences

- How it's built: the node-classes floor spec, *Reading inputs*.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "a sink acts only from its Run button or an explicit CLI run, and loads disarmed" by Claude.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
- 2026-09-24 Rewritten for a human reader at the author's ask; the rule is unchanged. The mechanics (`data()` preview, `run()`, the `enabled` flag left out of `extractInit`, `scripts/run-graph.ts --run`) moved to `floors/node-classes.md`. By Claude Opus 5.5.
- 2026-09-24 title changed from "A sink acts only from its Run button or an explicit CLI run, and loads disarmed" by Claude Opus 5.5, authorized by the author.
- 2026-09-24 re-parented from [B1, B12] to [B18, B1] by Claude Opus 5.5.
- 2026-09-24 Quality pass toward ratification: Why now argues from B18. By Claude Opus 5.5.
