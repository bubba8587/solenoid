---
id: B12
title: "A saved document reopens exactly as it was left; a save never silently drops data"
status: active
parents: ["[[A1]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
contested_by: Claude Opus 5
name: losslessSaves
aliases: [losslessSaves]
---

## Decision

Whatever a save captures, a load restores and the next save re-captures identically, and every save path fails loudly rather than writing a valid-looking file that has lost something. The document's text form is the canonical projection every save derives from.

## Why

[[A1]]'s calculator is only usable if the work survives. Every save-path failure found to date wrote a valid-looking file and surfaced only on reload, the worst kind of loss because nothing marks it when it happens.

## Consequences

Parent of the save-path rules: [[C29]] plainJsonInit, [[C30]] saveViaTextForm, [[C31]] immutableDocStore, [[C32]] autosaveSlotOrder, [[C33]] saveBindsMain, [[C34]] classNameIsType, [[C35]] unknownViaPlaceholder, [[C36]] captureBeforeSwap, [[save-format#Every field is persisted or deliberately transient]], [[C37]] observerOwnsSize.

- The loader opens exactly one format version. A newer file is refused rather than opened with its unknown fields dropped, because the next autosave would write that loss over the good copy; an older one is refused because there is no migration ([[B7]] preAlphaBreakFreely).
- A malformed file is refused before the live graph is torn down, and a failed rebuild rolls back to the snapshot; if the rollback also fails, autosave stays suspended so the wreckage never overwrites the last good autosave.

## Alternatives considered

### Contest 2026-09-15 by Claude Opus 5: keep wins

Rubric: [[A1]].
- keep: saves are lossless and loud. Cost: the persistence fixed-point and field-classification sweeps, slot rotation, text-form completeness scan.
- opposite: best-effort saves; occasional loss accepted for speed of change. A calculator that loses a setting on reload is not usable for real work.
- deletion: [[A1]] alone does not name persistence; the save-path rules would hang on the engine or the view, neither of which is why they exist.
- variant: lossless only for values, settings may reset. The recorded bug (an as-of join direction resetting on reload) is this variant's cost.
Verdict: keep.

## History

- 2026-09-15 contested by Claude Opus 5; keep won.
- 2026-09-18 name changed from "(unset)" by Claude Fable 5.1.
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-23 Version gate and rollback reasons added by Claude (lifted from src/graph/persistence.ts).
- 2026-09-24 Links to leaves that moved into specs now point at the spec sections, on the author's ruling; the rule is unchanged. By Claude Opus 5.5.
