---
id: B13
name: aiInScope
title: "the AI layer is in scope; an AI edit takes the same validated, approved path as a human's"
status: active
parents: ["[[A1]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
contested_by: Claude Opus 5
aliases: [aiInScope]
---

## Decision

The AI layer is IN scope. An AI edit proposes through the same governed, validated path a human edit takes, with an approval step. Provider: Anthropic (`aiService.ts`).

## Why

Reverses the old #7/#19 ruled-OUT. The cage framing survives as the design rule. Positioning stays file-over-app; AI is a capability, not the pitch, and marketing of it stays minimal.

## Consequences

- The model's grounding is generated from `nodeCatalog.ts` and the live classes (`aiGrounding.ts`), never hand-written ([[C8]] declareOnce), and is built once per session, so the system prompt stays byte-identical and the provider's prompt cache hits.
- The assistant ships switched off (`AI_ENABLED` in `aiKey.ts`, the author's call) until its verification tail is finished. Off, the sparkle, the Settings section and the What's New slide all hide; turning the flag on restores the whole surface.
- **Reopen if:** the loop can't be made trustworthy, or the feature distorts the Excel-refugee focus into an AI-first pitch.

## Alternatives considered

### Contest 2026-09-15 by Claude Opus 5: keep wins

Rubric: [[A1]].
- keep: AI edits in scope, caged behind validation and approval. Cost: the AI service, the whole-document rewrite gate and diff.
- opposite: AI stays out. Forgoes AI-assisted graph building for a calculator whose canvas is already a text form an AI can write.
- deletion: [[A1]] alone does not decide whether an AI may edit the document; each AI feature would re-open the question.
- variant: AI as a separate assistant that never edits the document. Loses the edit loop; the cage already bounds the risk.
Verdict: keep.

## History

- 2026-09-15 contested by Claude Opus 5; keep won.
- 2026-09-23 Grounding and ship-off ruling added by Claude (lifted from src/graph/aiGrounding.ts, aiKey.ts).
