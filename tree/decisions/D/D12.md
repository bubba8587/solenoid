---
id: D12
name: dateValuedPortIsDateTyped
title: "A port that carries a date is typed date"
status: active
parents: ["[[C10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [dateValuedPortIsDateTyped]
---

## Decision

**MUST:** a socket whose value is a date uses the date family (`date`, `datecombo`, `datelist`, `datetable`), never `number` or `numlist` on the grounds that a date serial is numerically a number. A wildcard rung isn't a violation, since it has committed to no family at all.

## Why

The socket type is the only thing that says "date" once the value is running. A date serial and a plain number are the same `number` at runtime, so once a date rides a numeric port, nothing downstream can tell them apart: Cast's date-aware text conversion, the FC's date styles and the type-default display all read the socket, not the number. Typing it correctly also gets the typeable date-list editor for free, which is why this rule brings `stringLiterals` with it (see [[C28]] literalsIffEditable). XIRR and XNPV once took their `dates` on a `numlist` labeled "Date serials", the only two such ports against more than 60 correctly typed ones.

## Consequences

*Exception:* a port whose label only mentions time while holding a count or an index ("Start period", "Days", "Coupon rate") is a number and stays one. The guard anchors on a label ending in date or dates for exactly this reason. Removed if port semantics are ever declared directly instead of inferred from the label.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
