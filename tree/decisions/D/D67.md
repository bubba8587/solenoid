---
id: D67
name: grammarOnlyAtBorder
title: "Link grammar and vendor link codes are parsed at the border; the engine keys predecessors by name"
status: active
parents: ["[[C70]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Fable 5.1
date: 2026-09-18
ratified_by:
confidence: high
aliases: [grammarOnlyAtBorder]
---

## Decision

Inside the engine, a predecessor is a structured record keyed by task name (`{task, type, lag}`). The `3FS+2d` link grammar, the vendors' integer link-type codes (MSPDI, .gan and XER each number them differently), and row numbers or UIDs are parsed at each importer and never become an internal key or a stored string.

## Why

Three vendors number link types three different ways, and a row number breaks as soon as the table is sorted or filtered, while a name survives both. The author also ruled out lists stored as strings inside a cell ([[schedule-and-gantt]]).

## Consequences

- An importer is a parser to the one record shape, and an exporter renders from it.
- **Reopen if:** a plan needs links to tasks that have no name. Ids would then be minted, and they would still not be row numbers.

## History

- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-23 Pointer moved by Claude Opus 5.5 to [[schedule-and-gantt]]; the rule is unchanged.
