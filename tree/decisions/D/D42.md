---
id: D42
name: perInputUnitBlind
title: "A node that doesn't do unit math gets plain numbers, input by input"
status: active
parents: ["[[C25]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
contested_by: Claude Fable 5.1
aliases: [perInputUnitBlind]
authorized_by: the author
---

## Decision

**MUST:** a node that doesn't do unit math gets plain numbers, never values with units. Only a node that says it does unit math sees the units, so a new node that does unit math has to say so.

## Why

Without this, a value with a unit reaches a node that compares it as a plain number, and `5 km > 3` answers wrong. Removing units in one shared place, rather than in each node, means a new node can't forget to. How it's built: the compute-pass spec (the unit boundary).

## Alternatives considered

### Contest 2026-09-16 by Claude Fable 5.1: keep wins

Judged by [[C25]] alone. keep: the strip is central (coerceInputs) and a node opts in per input, so a unit-blind node never sees a tag and an algebra node sees every tag; cost = one flag per algebra node, pinned by a source scan. opposite (every node receives UnitCells): every data() in nodes/ and packs/ must unwrap by hand, the 5 km > 3 regression returns at each site. deletion: [[C25]] says only the algebra changes a unit but not where the boundary sits; the strip point would be re-derived per node. Acted under it: TriangleSolverNode sets unitAware to read angle cells in degrees.

## History

- 2026-09-16 contested by Claude Fable 5.1; keep won.
- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
- 2026-09-24 Rewritten for a human reader at the author's ask; the rule is unchanged. By Claude Opus 5.5.
- 2026-09-24 title changed from "The unit-blind boundary is PER-INPUT" by Claude Opus 5.5, authorized by the author.
