---
id: D45
name: maxRankMatrix
title: "The value grammar: primitive or tagged scalar, list or matrix; nothing deeper, no bare-array scalar"
status: active
parents: ["[[C24]]"]
supersedes: ["[[D44]]"]
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [maxRankMatrix]
---

## Decision

**MUST:** a runtime value is one of four shapes:

- a primitive scalar;
- a tagged scalar, meaning a value that is one thing but needs more than one JS primitive to carry it, always a tagged object: `SolError` (`{__solError…}`), `UnitCell`, complex (`{__cx, re, im}`);
- a 1-D `Array` of cells;
- a 2-D `Array` of row `Array`s.

No scalar is a bare array, so `Array.isArray` at two depths is the complete rank test, and no code carries a private way of sniffing shape. Depth 3 or more is not a value: a surface that meets one answers `#SHAPE!`. Anything deeper than a matrix is a Cube, which is a container, not a value shape.

## Why

A bare-array scalar collides with the list representation, and every consumer that checks shape then needs its own way to tell the two apart. Complex numbers were once an `[re, im]` tuple, the only bare-array scalar in the model, and they forced four such workarounds: complex.ts's own broadcaster, special cases in `coerceInputs`, array canonicalization in `setKey`, and `ArrayChip.is2D`, where a complex list reaching a generic chip rendered as a two-column table. With one grammar, [[C15]] matricesInFormulas could admit matrices without wrapping them in a branded value type, and every new nesting scheme would reopen the ambiguity this closes.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Bare rule names linked as `[[ID]]` wikilinks by Claude; no wording or rule changed.
- 2026-09-22 Duplicate *Why:* label removed by Claude; no wording or rule changed.
- 2026-09-22 Absorbed D44 tagSpecialScalars: the tagged-scalar rule is the half of the rank grammar that keeps Array.isArray meaning list. Dropped a broken Enforced-by remnant from Consequences; by Claude.
- 2026-09-22 title changed from "The rank grammar: nothing nests deeper than a matrix" by Claude.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
