---
id: D7
name: oneMetricImpl
title: "A gating metric has one implementation and answers one question"
status: active
parents: ["[[C8]]", "[[C9]]"]
supersedes: ["[[D8]]"]
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [oneMetricImpl]
---

## Decision

**MUST:** a number that gates CI is computed in one module and answers one question. The human-readable report and the ratchet test call the same function, and neither recomputes it. A metric that would answer two questions is split into two fields: "is this Excel name callable" is never derived from "is this node reachable somehow".

## Why

A report that measures differently from its test is how a ratchet stops ratcheting without anyone noticing. The rule began as a comment and was broken twice in one session anyway. The report matched a leaf by its host's label and listed nine registered `FILL*` functions as a gap, and a gap computed as `!inFormula` let `SCAN` drop out of a gap it was still in. A metric that folds two questions together also lets an improvement on one hide a regression on the other.

## Consequences

- The node-to-formula coverage figure divides by the in-scope leaves, never the whole catalog, by the author's ruling: sliders, notes, sinks and chrome were never candidates for a formula name, so a whole-catalog denominator understates coverage and answers a different question. The excluded population is reported on its own line.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Absorbed D8 oneThingPerMetric (one question per metric), which it restated from the other side; by Claude.
- 2026-09-22 title changed from "A gating metric has exactly one implementation" by Claude.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-23 Coverage-denominator consequence added by Claude (from the comment lift).
