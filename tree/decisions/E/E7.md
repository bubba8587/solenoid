---
id: E7
name: trueanyNeedsPassthrough
title: "A `trueany` output implies a `passthrough()` declaration"
status: active
parents: ["[[D15]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [trueanyNeedsPassthrough]
---

## Decision

**MUST:** a class with a `trueany` output either declares `passthrough()` or is listed with the reason its type resolves another way: the FC is the resolver, Conduit lanes resolve through `conduitTrace`, composite boundary ports sync in their own pass, and XLOOKUP and NA are unknowable. `passthrough()` is the one declaration all five derived-type consumers read: trueany adoption, unit flow, the frame-shape resolver, the display-type walk, and coerceInputs' keep-tags boundary.

## Why

A forwarder that skips the declaration leaves its output `trueany` forever. Downstream FCs then can't key a family, and a date serial silently shows as its raw number.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the leaf and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-24 "node" reads "leaf" where it means a tree item, on the author's word; the rule is unchanged. By Claude Opus 5.5.
