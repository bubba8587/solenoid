// [[C107]] obsidianPlugin
// Only the Fx column's formula field reads this, and a property has no computed columns.
export function tokenAtCaret(_src: string, _caret: number): { word: string; start: number } | null {
  return null;
}
