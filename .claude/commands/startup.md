Start the Solenoid Vite dev server on port 1420 in the background, then confirm it's running.

Run: `npm run dev` in the project root as a background process.

After starting, tell the user the app is available at http://localhost:1420
