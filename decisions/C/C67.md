---
id: C67
name: mdbaseCeiling
title: "mdbase: read the schema, validate on write, nothing more"
status: active
parents: ["[[B1]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [mdbaseCeiling]
---

## Decision

Vault Folder types a column from a matching mdbase type FIRST (`mdbaseTypes.ts`, above `.obsidian/types.json` and the guesser), and Write Properties refuses a row that breaks a type / enum / min / max / required rule (`vaultCube.ts` `mdbaseSchemaFor` + `validateAgainst`). Both fall through silently when no schema exists. The ceiling: no Solenoid-authored type files, no type-file writer beside the `.base` writer, no query passthrough to the mdbase binary (bundle item H, HOLD).

## Why

Author 2026-09-07d. mdbase is an optional schema file beside a folder of notes, not how TaskNotes records its data: TaskNotes writes ordinary frontmatter, and Solenoid reads it (the plugin's API or the disk) whether or not a schema exists, so [[B1]]'s integration needs only to respect a schema when one is present. The spec breaks by policy before 1.0, so an unreadable schema means the guesser, never a refused folder.

## Consequences

- A blended Solenoid + TaskNotes `_types/` schema is the user's to write, and works today.
- The pitch leads with Obsidian + TaskNotes; mdbase gets one clause ("respects an mdbase schema when the folder has one").
- **Reopen if:** mdbase reaches 1.0, or users arrive with schemas and ask; a type-file writer is then the `.base` writer's size.
