---
id: C29
name: plainJsonInit
title: "`extractInit` is a fixed point, and JSON-plain"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [plainJsonInit]
---

## Decision

**MUST:** for every catalog node, `extractInit(new Ctor(extractInit(n)))` equals `extractInit(n)`. What a save captures, a load re-applies, and the next save captures again identically, including non-default booleans and changed literal maps. Every captured value must also survive `JSON.parse(JSON.stringify(…))`.

## Why

A field that is captured but not re-applied drops on reload, and nothing catches it ([[B12]] losslessSaves). The JSON half closes a second blind spot: the file path stringifies each field, so a `Map`, `Set`, class instance or `Infinity` in a config silently empties in the saved file while the live-object round trip still passes.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
