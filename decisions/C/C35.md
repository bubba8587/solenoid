---
id: C35
name: unknownViaPlaceholder
title: "An unknown node type round-trips losslessly through Placeholder"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [unknownViaPlaceholder]
---

## Decision

**MUST:** loading a save with an unregistered type builds a `PlaceholderNode` that carries the original type, init, literal maps and derived socket keys, and re-saves as that original type, never as "PlaceholderNode". Its outputs emit `#REF!`, never a silent blank.

## Why

The dormant-pack story rests on this: open a document with a pack off, save it, and the pack's nodes must survive intact ([[B12]] losslessSaves). Keeping the original type and init makes the round trip lossless, and `#REF!` keeps the break loud while it lasts.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
