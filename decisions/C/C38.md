---
id: C38
name: sinkRunButtonOnly
title: "A sink acts only from its Run button or an explicit CLI run, and loads disarmed"
status: active
parents: ["[[B1]]", "[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [sinkRunButtonOnly]
---

## Decision

**MUST:** a node with an irreversible external effect (a disk write) never acts from `data()`. `data()` computes a preview only; the effect lives in `run()`, which fires only from the node's Run button or an explicit CLI run (`scripts/run-graph.ts --run <label>`, which stands in for the button), and only while the `enabled` arm flag is on. The arm flag never persists: it is excluded from the `extractInit` whitelist, so every load path (reopening a save, paste, Placeholder restore) starts disarmed.

## Why

An irreversible effect has to be a deliberate act. Firing from `data()` would write to disk on any recompute, and a saved arm flag would let merely opening a shared file write to the reader's disk. Vault writes ([[B1]] obsidianBet) and safe reopening ([[B12]] losslessSaves) both depend on this.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "a sink acts only from its Run button or an explicit CLI run, and loads disarmed" by Claude.
