---
id: C11
name: socketBox12
title: "A socket is a fixed 12×12 box in its own measured row"
status: active
parents: ["[[B10]]"]
supersedes: ["[[C12]]"]
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [socketBox12]
---

## Decision

**MUST:** every socket renders as a fixed 12×12 box (`display:block; line-height:0`) inside its own measured row, and its vertical position comes from measuring that row: never a fixed constant, and never a `transform`.

When a card has two or more sockets on one side, each gets its own row: `InlineInputs`, `InlineOutputRows` or `MeasuredSocketRow`, or a card's own measured row (as the Equation card's `EquationVarRow` and `EquationOutRow` are). `NodeShell` lays out output sockets only, and only as bare dots; a card that declares inputs renders them itself.

## Why

React Flow places a cable's endpoint by measuring the Handle box that wraps the socket (`flow/FlowSocketHandle.tsx`). A `transform` or an unmeasured constant makes that measurement report the wrong spot, so the cable ends beside the dot instead of on it.

A socket with no row of its own gets no position of its own: `NodeSocket` falls back to `--out-socket-top, 50%`, so every dot on that side lands on the same pixel, one visible handle with the rest stacked under it, and nothing throws. Geocode once shipped with `lat`, `lon`, `timezone` and `label` on one point, and Weather with `lat` and `lon` declared but no dot to plug into, so the two couldn't be wired to each other at all.

## Consequences

- How a socket anchors and how each row is measured: `specs/react-flow-surface-contract.md`.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Duplicate *Why:* label removed by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 Absorbed C12 socketRows (one measured row per socket), the other half of the same placement rule; by Claude.
- 2026-09-22 title changed from "The socket box is a deterministic 12×12" by Claude.
