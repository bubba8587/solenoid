---
id: C75
name: gpuTextureBudget
title: "a covered canvas stops painting; layer promotion never overruns a mobile GPU"
status: active
parents: ["[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [gpuTextureBudget]
---

## Decision

A live canvas under a full-viewport opaque overlay stops painting: the composite drill-in sets `visibility: hidden` on the covered app canvas, and any future canvas-covering surface owes the same. The HTML-canvas layer gives the viewport a gesture-scoped `will-change: transform` layer on fine pointers only, NEVER on coarse (`IS_COARSE`, not `IS_MOBILE`); on coarse pointers only the small DOM-only elements are promoted, per element, size-capped at 1024px (`PROMOTE_MAX`).

## Why

An occluded subtree is not reliably dropped from raster, so a covered canvas keeps its layers and textures resident: two surfaces' worth on a mobile GPU whose max texture the canvas bbox × dpr can already overrun on its own. A tablet runs the desktop UI on a mobile-class GPU, where a viewport-sized layer fails tile allocation, so the gate is the pointer type, not the device class.

## Consequences

- It must be `visibility`, not `display: none`: the covered area is still asked to re-render and re-measure cards while drilled in, and measurement needs a real layout box.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the node and `dte show` derives "implemented by" (A8 threeLayers upstream).
