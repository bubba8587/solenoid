---
id: C45
name: excelComparisons
title: "comparisons match like Excel's `=`; identity ops are case-sensitive keys"
status: active
parents: ["[[B5]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [excelComparisons]
---

## Decision

Every comparison (`=`, Comparison node, lookup match, Filter text ops) is case-INsensitive (EXACT / "Match case" is the escape hatch). Every identity op (Join/Group By/Distinct keys) is case-SENSITIVE. Corollary (same line): list UNIQUE never dedupes error cells; frame Distinct dedupes them by code.

## Why

[[B5]]: Excel's `=` ignores case, so comparisons do too. Keys are different: silent case-merging destroys distinctions in the data.

## Consequences

- **Where:** `frameVerbs.ts`, `engine.rs`, the P6 operator table.
- **Reopen if:** nothing; a per-verb case-fold OPTION would be an addition inside the rule.
