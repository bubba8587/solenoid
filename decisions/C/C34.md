---
id: C34
name: classNameIsType
title: "The class name is the persisted type: kept and unique"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [classNameIsType]
---

## Decision

**MUST:** `constructor.name` is the `type` written into every save, the key that loads resolve through in the constructor registry, and a dispatch key elsewhere (`SEES_ERRORS`, group collapse, pins). Therefore:
- production builds keep class names. Vite 8 defaults to the Oxc minifier, which has no keepNames equivalent, so `vite.config.ts` pins `build.minify: "esbuild"` and keeps `esbuild: { keepNames: true }`;
- no two catalog classes share a name.

## Why

The class name is the saved identity ([[B12]] losslessSaves). A mangled name breaks every load. Two classes sharing a name are worse, because the registry keeps the first one registered: saves of the other class silently rebuild as the winner, with different sockets, plausible wiring and the wrong computation. The Placeholder path fires only for an absent type, and a collision looks exactly like a match.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
