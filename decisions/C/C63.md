---
id: C63
name: oneRecordNode
title: "the Record family is ONE figure node, and its views are ARGUMENTS"
status: active
parents: ["[[B11]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [oneRecordNode]
---

## Decision

Every record-shaped view (Card, Gallery, Board, and any future one) is an op on the one Record node, never a sibling node. The view selector is **argument-kind** in `NODE_OPS` even though it names the card. Record-at-a-time ENTRY is the Table popup's Form view on frame-source editors: the same record look made editable, placed by the same layout text, authored on the Frame Input CARD (never in the popup), with column-type entry widgets (logical → checkbox with an indeterminate blank; date → the native date input writing ISO text).

## Why

Author order, 2026-08-18 (maximalMerge). A view is a presentation parameter of one figure, not a thing called by name, unlike Chart, whose TYPES are operations.

## Consequences

- "gallery"/"kanban" ride the host leaf's keywords; no per-view search rows or formula names exist.
- The card itself never draws its grid (squished at card width): the hero chip carries it, and the drawn card lands wherever the chart output goes (Display, popup, Report embed). Record boxes TOUCH and are square.
- **Reopen if:** a record view needs behavior an op cannot express, or the formula surface ever needs a record-view name.

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the node and `dte show` derives "implemented by" (A8 threeLayers upstream).
