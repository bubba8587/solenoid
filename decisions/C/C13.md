---
id: C13
name: frameLabelGrammar
title: "Frame-input labels follow the column-role grammar"
status: active
parents: ["[[B14]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [frameLabelGrammar]
---

## Decision

**MUST:** a Frame or 2-D input socket's label states which columns the input expects, in one grammar:
- **Expects specific columns:** the column roles in expected order, joined by `" + "`, each a Title-case word: `From + To + Value`, `Label + Value`, `Date + Value`. A standard column group may compact to its standard initialism (`Date + OHLC`), never spaced-out single letters.
- **No specific columns** (any Frame, or homogeneous columns): one plain Title-case noun (`Frame`, `Data`, `Series`, `Scores`).
- **Never a shape parenthetical** like `(2-D)` or `(list)`. The socket glyph and the Socket Legend already state the shape, and restating it breaks DESIGN.md §7.

Exception: the λ-table inputs in `nodes/tableLambda.ts` carry the λ argument name and optionality (`Values (value)`, `value2 (optional)`). That's an argument-binding hint, not a shape, and it's scoped to that file.

## Why

Aligned parallel columns arrive as one Frame input by design, so the label is the only place the expected columns can be read before wiring. Two ad-hoc dialects (`From + To + Value` vs `Series (2-D)`) made that one important hint impossible to learn.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
