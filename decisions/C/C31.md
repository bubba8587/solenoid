---
id: C31
name: immutableDocStore
title: "`documentStoreCore` transforms are structurally immutable"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [immutableDocStore]
---

## Decision

**MUST:** every transform returns a NEW `SolDoc` for each document it changes.
`documentStore.persist()` decides what to write by OBJECT IDENTITY
(`_lastPersisted.get(id) === doc` skips the write), so an in-place mutation still updates
the screen but is silently never persisted — the edit vanishes on the next reload.

## Why

[[B12]]: `documentStore.persist()` decides what to write by object identity, so an in-place mutation updates the screen but is never saved, and the edit vanishes on the next reload.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
