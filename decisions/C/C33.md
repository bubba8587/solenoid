---
id: C33
name: saveBindsMain
title: "Persistence binds MAIN, never the active surface"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [saveBindsMain]
---

## Decision

**MUST:** the composite drill-in substitutes editor/area/history through the
`activeGraph.ts` seam for CANVAS operations only; `getEditor()`, serialization, autosave
and load resolve the MAIN graph unconditionally. A save taken while drilled in must
serialize the document, not the open subgraph.

## Why

The failure is total, silent data loss — an autosave during a drill-in would
write the composite's internal subgraph OVER the document, and the file would be valid.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Duplicate *Why:* label removed by Claude; no wording or rule changed.
