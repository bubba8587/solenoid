---
id: C37
name: observerOwnsSize
title: "`width`/`height` ownership: the observer owns them; size-owners re-consume"
status: active
parents: ["[[B12]]", "[[B10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [observerOwnsSize]
---

## Decision

**MUST:** `node.width` and `height` are a measurement mirror: NodeCard's ResizeObserver
overwrites both with rendered pixels on every layout, and the minimap silhouette and cable
geometry read them. Consequences:
- They persist for every node, but only the declared size-owner classes (user-dragged
  surfaces: the annotation frames, the composite card, groups, the overlay hosts)
  re-consume `init.width/height`; for everyone else the persisted values are inert
  and the class default + re-measure win;
- A class with a user size gesture must either re-consume the init or route through
  `nodeSizeStore` (Display's grip — its own persisted channel, `sn.size`);
- Nothing else may start consuming the init.

## Why

Dual-use fields drift silently in both directions. A new resizable class that forgets to
re-consume loses the user's drag on reload (the same class of bug as `asofDirection`, [[D50]]
everyFieldClassified), and a compute class that starts consuming freezes a measured size into
its layout.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
