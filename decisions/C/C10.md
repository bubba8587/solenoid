---
id: C10
name: socketLattice
title: "type separation and dimensional flow, derived from the family × rank product"
status: active
parents: ["[[A6]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [socketLattice]
---

## Decision

Element families never auto-cross (Cast required; sole bridge `logical↔number`); values flow freely up in rank (scalar→list→matrix→frame). The lattice is a family × rank product, and its legality rules are DERIVED from that product rather than enumerated per pair.

## Why

[[A6]]: a value's type is part of its meaning, so the socket lattice refuses connections that would silently coerce. Deriving legality from the product keeps every variant consistent without hand-listing pairs.

## Consequences

- **Reopen if:** a new family or a second bridge: re-derive the lattice + sweep, never ad-hoc edit.

## History
- 2026-09-18 Where pointer removed by Claude Fable 5.1: the spec, the code and the sweep cite the node, so `dte show` derives them (A8 threeLayers upstream). specs/socket-lattice.md written from this subtree.
