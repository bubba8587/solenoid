---
id: C30
name: saveViaTextForm
title: "Every save passes through the text form, so it carries every SavedGraph field both ways"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [saveViaTextForm]
---

## Decision

**MUST:** every save passes through the text form: `serializeGraph()` returns `readTextForm(writeTextForm(raw))`. So every top-level `SavedGraph` field is written by `writeTextForm` and read back by `readTextForm`.

## Why

A field that either direction omits is deleted from every save, and autosave then writes the lossy result over the good copy ([[B12]] losslessSaves). That happened to `comments` and `reportPalette`: `buildRawSavedGraph` built them and the round trip dropped them, losing real data on every save until the gap was found.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
- 2026-09-22 title changed from "The text form is the narrow waist: every `SavedGraph` field, both directions" by Claude.
