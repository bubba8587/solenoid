---
id: C30
name: saveViaTextForm
title: "The text form is the narrow waist: every `SavedGraph` field, both directions"
status: active
parents: ["[[B12]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [saveViaTextForm]
---

## Decision

**MUST:** `serializeGraph()` returns `readTextForm(writeTextForm(raw))`, so every
top-level `SavedGraph` field must be written by `writeTextForm` and read by
`readTextForm`. A field either direction omits is deleted from every save, and autosave
then writes the lossy result over the good copy.

## Why

`comments` and `reportPalette` were built by `buildRawSavedGraph` and then silently dropped
by the round trip, losing real data on every save until the gap was found.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
