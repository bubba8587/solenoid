---
id: E10
name: pickVsAggregateErrors
title: "Positional access filters errors per cell; aggregation propagates whole"
status: active
parents: ["[[D35]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [pickVsAggregateErrors]
---

## Decision

**MUST:** a positional lookup (INDEX, XMATCH, MAKEARRAY reads) propagates ONLY the error
of the cell it actually references — an unreferenced error cell elsewhere in the range
must not blanket-error the call. An AGGREGATE over a range containing an error still
propagates it whole (Excel-correct). Both directions are silently wrong when flipped:
blanket-erroring hides a good answer, under-propagating hides a bad one.

## Why

Excel semantics ([[A5]]): a lookup answers from the cell it references, an aggregate over a range answers for the whole range. Flipping either direction hides a good answer or a bad one.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
