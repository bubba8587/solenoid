---
id: E2
name: cubeNeverNarrowsToFrame
title: "A cube never enters a frame socket; the node gets the wider socket"
status: active
parents: ["[[D13]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [cubeNeverNarrowsToFrame]
---

## Decision

**MUST:** the lattice refuses cube → frame. A verb that should accept a cube declares a
cube-adoptive INPUT (`cubeAdoptIn` + `noWidenInputs`, the Add Column / Computed Column
pattern) and flattens or refuses inside `data()` (`flatCubeToFrame`: a flat cube is rows, a
nested cell is a loud `#SHAPE!` naming the column). Never widen the lattice to spare a node.

## Why

*Author's words (2026-09-12, evidence per [[C7]] authorRuled, not ARR):* "cube into frame socket is NOT acceptable. if you have to upgrade
something, upgrade the sockets on each node, not vice versa."

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Bare rule names linked as [[ID]] wikilinks by Claude; no wording or rule changed.
