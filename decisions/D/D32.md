---
id: D32
name: refreshOutsideRebuild
title: "A live-data refresh never runs inside a rebuild scope"
status: active
parents: ["[[C23]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [refreshOutsideRebuild]
---

## Decision

**MUST:** `refreshConnection` (the manual button and the interval timer) drives its
recompute OUTSIDE `beginGraphRebuild`/`endGraphRebuild`. (Bulk TOPOLOGY operations —
load, paste, undo/redo, composite create/unpack, sweeps — wrap rebuild scopes
deliberately; the rule binds the REFRESH path, which must never be one of them.) A refresh that lands inside a rebuild scope silently swallows the
edge-detection downstream: an Alert watching refreshed live data simply stops firing.

## Why

A rebuild scope suppresses edge-detected effects (effectsEdgeTriggered) so loads don't replay alerts; a refresh inside one would silently swallow a real alert on fresh live data.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
