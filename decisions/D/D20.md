---
id: D20
name: declareContract
title: "A registration declares its full contract"
status: active
parents: ["[[C17]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [declareContract]
---

## Decision

**MUST:** every `registerInternal` name has an `EXCEL_IMPL_META` entry declaring
`returns`, `arity`, and where applicable `rank` and `listArgs`. Routing DERIVES from that
entry (`noManualList`); it is never declared twice.

## Why

Routing, rank checks and list handling all derive from the meta entry ([[D4]] noManualList); a registration with no declared contract has nothing to derive from, so its routing would be guessed or hand-kept.

## Consequences

*Exceptions:* the POSITIONAL lookups (`XLOOKUP`/`XMATCH`/`INDEX`) declare meta but not
`listArgs` — they are routed by `RANGE_POSITIONAL` (skip the error scan) and the flag
would reroute them. **Removed by:** unifying the two routing declarations.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Bare rule names linked as [[ID]] wikilinks by Claude; no wording or rule changed.
