---
id: D2
title: Local File reads a bundled CSV through the demo seam, so the Excel-over-CSV scene works on web
status: active
parents: [C1]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 4.8
date: 2026-09-14
ratified_by:
confidence: high
---

## Decision

LocalFileNode resolves its data folder via getCsvFolder and allows a demo-vault folder off-desktop (CSV only; Parquet still needs the native engine). readFileText was routed through the path-aware fs() dispatch so a demo path reaches the in-memory provider.

## Why

C1 makes vault-relative reads work on web; extending it to the Local File data folder lets the Excel-over-CSV marketing scene read a real bundled expenses.csv rather than a mockup.
