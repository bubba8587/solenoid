---
id: D59
name: oneReadbackPerPaint
title: "the atlas packer takes one GPU read-back per paint, never one per node"
status: active
parents: ["[[C42]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [oneReadbackPerPaint]
---

## Decision

Each paint's captured nodes pack into one atlas region; the whole paint is snapshotted with ONE canvas read-back, and per-node textures come from bitmap→bitmap crops (`createImageBitmap(atlas, x, y, w, h)`, no canvas read-back).

## Why

Snapshotting each node's raster with its own `createImageBitmap(canvas, region)` is a canvas read-back PER NODE (~16 per paint), the expensive pattern on mobile GPUs. The shelf packer exists solely to collapse them.

## Consequences

## History

- 2026-09-18 Where pointer removed by Claude Fable 5.1: the implementing files cite the node and `dte show` derives "implemented by" (A8 threeLayers upstream).
