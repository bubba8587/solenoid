---
id: D34
title: oneErrorKind: One notion of error
status: active
parents: [C24]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
---

## Decision

**MUST:** failures flow as a tagged `SolError`. `ISERROR` ⟺ `IFERROR`; the `#N/A` test is
centralized as `isNaError`. A bare `NaN` is not an error.

## Why

Two notions of error make ISERROR and IFERROR disagree about the same cell, and a bare NaN counted as an error in one place and a number in another is a silent wrong answer (arraySemantics).

## Consequences

*Enforced by:* `errorValue.test.ts` → "ISERROR (Test) and IFERROR agree: only a tagged
error counts (a bare NaN does not)". (The centralization clause itself is a reading
rule — nothing fails if a second local `#N/A` comparison appears.)
