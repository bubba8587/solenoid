---
id: D44
name: tagSpecialScalars
title: "A special scalar is a TAGGED OBJECT, never a bare array"
status: active
parents: ["[[C24]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [tagSpecialScalars]
---

## Decision

**MUST:** every non-primitive scalar, a value that is one thing but needs more than one JS primitive to carry it, is a tagged object: `SolError` (`{__solError…}`), `UnitCell`, complex (`{__cx, re, im}`). No scalar is a bare array, so `Array.isArray` means exactly one thing everywhere: this is a 1-D list.

## Why

A bare-array scalar collides with the list representation, and every consumer that checks shape then needs its own way to tell them apart. The complex `[re, im]` tuple forced four: complex.ts's own broadcaster (tagging at the call site because `broadcastCells`' `Array.isArray` test couldn't tell a scalar from a list), `coerceInputs`' complex special cases, `setKey`'s array canonicalization, and `ArrayChip.is2D`, where a complex list reaching a generic chip silently rendered as a two-column table. Complex was the only bare-array scalar in the value model and the only reason "a cell may be an array" was ever true.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
