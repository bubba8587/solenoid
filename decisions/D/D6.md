---
id: D6
name: opRowDerivesFromHost
title: "An op's search row derives from its host leaf"
status: active
parents: ["[[C8]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [opRowDerivesFromHost]
---

## Decision

**MUST:** the Add-menu row for a hidden op is built from the host leaf plus the op's own declaration (`opEntry` spreading `...host`), never as a second hand-written catalog entry. What the row must not inherit is named at that call site, with the reason:
- the host's `keywords`, because family words would make every sibling op match the same queries and the rows would stop telling the ops apart;
- its `hiddenOps` and its ops mark, because a row that is one op has nothing folded inside it.

## Why

An op row is a view of its leaf, so every property the leaf owns (label stem, pack, accent, description) has to follow the leaf automatically, or the menu and the card drift apart ([[C8]] declareOnce). The properties it does not inherit are the exception list, and they belong in the same function rather than a parallel table ([[D3]] overrideInPlace). Blocking every `keywords` value, the op's own included, is what once pushed per-op Excel spellings into the visible label.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
- 2026-09-22 Prose rewritten by Claude; the rule is unchanged.
