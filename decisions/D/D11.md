---
id: D11
name: noAutoCross
title: "Type separation: element families never auto-cross"
status: active
parents: ["[[C10]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [noAutoCross]
---

## Decision

**MUST:** a value of one element family never connects to an input of another. Crossing
requires an explicit Cast node.

## Why

[[A6]]: a value's type is honest end to end. An automatic cross-family connection would coerce silently at the socket, exactly the plausible-but-wrong answer [[A6]] refuses; an explicit Cast node makes the conversion visible on the canvas.

## Consequences

*Enforced by:* `socketConnect.test.ts` → "CROSS-family is blocked everywhere EXCEPT
logical↔number (dim-mirrored)".
*Exceptions:* `logical ↔ number` is the ONE bridge (boolean ↔ 0/1), mirrored at every
rank. **Removed by:** nothing — it is the deliberate consequence of Excel treating
TRUE/FALSE as 1/0. Any *second* exception is a lattice design change, not a patch.
