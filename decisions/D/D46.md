---
id: D46
name: freezeVolatilePerCalc
title: "A volatile `data()` freezes its roll on the recalc generation"
status: active
parents: ["[[C23]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: medium
aliases: [freezeVolatilePerCalc]
---

## Decision

**MUST:** a node whose `data()` draws randomness caches the draw and re-rolls only when
`getRecalcGen()` changes. Bare `Math.random()` in `data()` re-rolls on EVERY recompute
pass, so any unrelated edit silently changes the value, F9 stops being the thing that
controls re-rolling, and a Monte Carlo built on it is non-reproducible.

## Why

calcModes: F9 is the control for recalculation. A draw that re-rolls on every recompute pass changes a value on any unrelated edit and makes a Monte Carlo non-reproducible.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
