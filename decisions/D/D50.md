---
id: D50
name: everyFieldClassified
title: "Every node field is persisted or DELIBERATELY transient"
status: active
parents: ["[[C29]]"]
supersedes: []
superseded_by:
conflicts_with: []
made_by: ai
by: Claude Opus 5
date: 2026-09-15
ratified_by:
confidence: high
aliases: [everyFieldClassified]
---

## Decision

**MUST:** every own field of every catalog node is one of three things: captured by `extractInit` (the whitelist, the literal maps, or a bespoke extras block); transient by naming convention (`cached*` derived display state, `_*` private runtime machinery); or listed in the `DELIBERATELY_TRANSIENT` map with the reason it must not persist. A field in none of these is an unmade decision, and the sweep fails by name.

## Why

The fixed-point sweep ([[C29]] plainJsonInit) proves whitelisted fields round-trip, but it can't see a field the whitelist never captured: both sides omit it identically, so the test passes while the user's setting silently resets on every reload. The first triage of every unclassified field found exactly that bug: `asofDirection`, an as-of Join's direction dropdown, reset to "backward" on every save, reload and paste.

## History

- 2026-09-18 Enforced-by pointer removed by Claude Fable 5.1: the enforcing test cites the node and `dte show` derives the list (A8 threeLayers upstream; [[C9]] labelUnenforced reworded).
- 2026-09-22 Bare rule names linked as [[ID]] wikilinks by Claude; no wording or rule changed.
- 2026-09-22 Prose rewritten for plain reading by Claude; the rule is unchanged.
