# Solenoid 2.0 — release plan (author-present flagships)

The genuinely-major arcs reserved for 2.0, each built WITH the author in a dedicated
session (decided 2026-07-06, organized 2026-07-08). Bundle-level detail lives in
[`v2.0/`](v2.0/README.md); this is the release view. The release tail is in
[`backlog.md`](backlog.md); the executed 1.2 queue is archived at
[`archive/1.2-plan.md`](archive/1.2-plan.md).

## Shipped since this view was cut

- **FC A4 — units by dimensionality** — ✅ SHIPPED 2026-07-12/13 (the value engine computes
  with dimensions end-to-end; the FC is value-mutating; matrices carry one homogeneous unit
  per unitGranularity; cubes per-cell). No longer a flagship-to-build. Live truth: `formatModel.ts`,
  subsystem-invariants "Unit flow", `decisions.md` unitGranularity; archived plan
  `archive/units-format-controller.md`. Author-present polish tails: `deferrals.md`.

## The flagships, in intended order

1. **Excel `.xlsx` transpiler** (`v2.0/08`). The import story: a workbook becomes a
   graph. Deliberately sequenced late; not started.
2. **compositeToolbarReroute — toolbar reroute** for the composite drill-in. The real top toolbar and mobile
   bar drive the active subgraph; finishes the first-class drill-in arc. Cross-cutting,
   wants live eyeballing.
3. **conditionalFormatting — conditional formatting for tables.** Needs its own design pass; the author's
   bar is that it must clear Excel's version by a lot. Display-node-only; must not step
   on FC format/units territory.

## Buildable bundles (no longer gated)

- **`v2.0/10` decision sensitivity** ("wiggle the weights") — the composite Monte Carlo
  run-mode hook it waited on SHIPPED 2026-07-12; now buildable (needs re-triage / an
  author pick, not a gate).
- **`v2.0/12` value-model extensions** — uncertain values + money mode; each needs an
  author representation call first. The as-of half already shipped in 1.1. (Monte Carlo's
  composite-scoped `UncertainNumber` now exists as a concrete precedent.)

## Verdict pending (fresh author call before any doc)

- **#23** persistent compute cache · **#35** MCP port. See `v2.0/README.md`.

## Not happening

The ruled-OUT list in `v2.0/README.md` stands; don't re-litigate it here.
