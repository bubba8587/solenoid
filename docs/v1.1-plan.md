# v1.1 — Detailed execution plan

The post-1.0 cycle. **v1.0 is released** — the desktop spine shipped (Tauri +
native Polars + the full relational verb set + the HTML-in-Canvas renderer) and is
tagged/live. v1.1 is the **deferred tail** the milestone split (2026-06-24) parked
so 1.0 could ship: the Format Controller redesign + the units architecture (the
flagship), the pack/extensibility surface, canvas/interaction polish, and a handful
of integration nodes. **This is now the active milestone** — work can begin.

This expands the archived `roadmap.md` "v1.1 — post-v1 deferred tail" bullet list
into a sequenced, file-level plan the same way `v1.0-plan.md` did for the spine.
The backlog stays the fine-grained checklist; this is the build order, the
dependencies, and the exit criteria.

**Development continues on `develop`** (the standing rule); `main` is the released
1.0 and is author-gated. The **bug lane** below is the natural first pickup.

---

## Shape of the release

Five workstreams, one always-on bug lane. Unlike v1.0 (a linear spine A→B→C→D),
v1.1's streams are mostly **independent** and can be picked up in any order. The
sequencing that matters is *within* a stream and at two hard gates:

| Workstream | Theme | Weight |
|---|---|---|
| **WS-A — Format Controller & Units** | the flagship: FC function model + visual redesign + units-by-dimensionality | large (own sub-milestones) |
| **WS-B — Packs & extensibility** | dormant-pack persistence gate, new core nodes, Timesavers, domain packs | medium |
| **WS-C — Canvas & interaction** | grid system, cable collision avoidance, hideable chrome, pinch-zoom, small polish | medium |
| **WS-D — Data & integration** | Obsidian sync, image bundling, finance connection, cube-cell XLOOKUP | medium |
| **WS-E — Engine scale niceties** | lazy-plan fusion, direct CSV→Polars reader, formula re-audit | small/opportunistic |

> **Verification note (grounded in code 2026-07-01).** Every item below was audited
> against the actual codebase (5 parallel read-only sweeps), because the source
> checklists had rotted — several "planned" items already shipped. Each item now
> carries a status: **`[x]` DONE**, **`[~]` PARTIAL**, **`[ ]` NOT DONE**, with
> file:line evidence on the ones that changed. Re-verify against code before
> trusting any status here.

**One hard gate remains:**
1. **WS-A's units architecture depends on the FC function model landing first** —
   the function model defines how one FC resolves precision × style × unit × type;
   units-by-dimensionality then generalizes "unit" from per-value to per-cell /
   per-column. Build the function model, then the units model.

(The former second gate — "no code pack before dormant-pack persistence" — is
**already satisfied**: `persistence.ts` placeholders unknown node types with cables
preserved and records `SavedGraph.packs` provenance. See WS-B1. Code packs can ship
safely now.)

Suggested cut order: **bug lane → WS-A (function model, then redesign) → WS-B gate
+ first pack → WS-C polish in parallel → WS-D/WS-E opportunistic.** WS-A is the
headline; everything else is fill-in.

---

## Bug lane (the first pickup)

Two dated author-flagged regressions. One is already fixed; one remains.

- [x] **Navigator group rows are blown out** (author 2026-07-01) — **FIXED on `develop`**
  (commits `5c65299` / `7dce600` and follow-ups). `OutlinePanel.tsx:485` now paints the row
  `background: hexToRgba(r.color, 0.24)` inside `border: 1px solid hexToRgba(r.color, 0.55)`
  (translucent tint + frame, matching the canvas group body), and `colorOf` (`:43-48`)
  resolves the palette slot via `resolveColor((n as GroupNode).color)` exactly like
  `GroupNode.tsx:238`. The solid full-strength bar is gone. Nothing to do.
- [ ] **FCs mis-dock to a frontmatter Note on load/switch** (author 2026-06-25, random-repro)
  — **STILL PRESENT, both mechanisms unfixed** (verified 2026-07-01).
  Leading hypothesis (investigation already ruled out ID collision, singleton
  contamination, and load-time re-docking): `findDockTarget`'s `DOCK_SNAP_PX=34` is in
  **screen px**, so zoomed out it covers a large canvas area and snaps an FC to a far-away
  Note's tall stack of output sockets during editing; that wrong dock persists and shows on
  reload. Confirmed: `DOCK_SNAP_PX = 34` (`Canvas.tsx:210`, comment says "screen px") is
  compared against `getSocketScreenCenter` distances (`:220-237`) with **no** division by
  `area.area.transform.k`. Fix: make the snap radius **zoom-aware / canvas-unit**, and prefer
  a host the FC is actually adjacent to. Also fix the confirmed ordering smell: `nodecreated`
  calls `dockSelf()` **unconditionally** (`Canvas.tsx:2272`) before persistence remaps
  `hostNodeId` (`persistence.ts:393-402`), so every FC docks twice on load, the first time
  against the stale saved id — guard the `nodecreated` call with `!isGraphRebuilding()` (the
  sibling branches at `:2274`/`:2314`/`:2321` already are; persistence's own post-remap
  `dockSelf` at `:424-425` handles the rebuild case). Diagnostic first: open an affected save,
  confirm the wrong FC's `init.hostNodeId` equals the Note's id (persisted-wrong vs transient).

---

## WS-A — Format Controller & Units (the flagship)

Author: "unit carrying and data-type segregation are some of the biggest value adds
of this project." This is the headline workstream. Four sub-parts, in dependency
order. `formatAnnotationStore.ts` (`FormatAnnotation`, `FormatStyle`), the FC popup,
`unitFlow.ts`, and the value renderers are the touch surface throughout.

### A1 — The function model (spec FIRST, then code) — `[ ]` NOT DONE

Confirmed per-style ad-hoc: `applyFormatStyle` is a hand-written `switch(style)` with
precision duplicated inside the `decimal`/`percent` cases (`formatAnnotationStore.ts:90-128`),
and the popup gates controls by scattered inline flags (`isDate`/`isText`, `format ===
"decimal" || "percent"`) at `FormatControllerNode.tsx:230-233`/`:371`. No unified model.

Pin down ONE coherent definition of how an FC decides what to render across every
axis it controls at once, replacing today's per-style ad-hoc logic on
`FormatAnnotation`. The axes that must compose:

- **precision** — `decimalDigits` × `decimalMode` (`"places"` fixed decimals vs
  `"sig"` significant figures): one resolution rule for how it interacts with each
  style (sig-figs on `percent` / `scientific` / `fraction`).
- **format style** — `FormatStyle` (`auto | decimal | integer | percent | fraction
  | fraction_adv | scientific`) + `customPattern`.
- **unit** — `unit` / `customUnit`, a SEPARATE axis applied after the numeric format
  (currency is a unit, not a format); must coexist with every style.
- **value type** — the same FC can sit on number / date / text / logical; the model
  defines which controls light up vs no-op per incoming type (sig-figs meaningless
  on a date; `textCase` meaningless on a number).

**Deliverable order (author-specified):**
1. [ ] **The spec** — a truth table: which controls apply per value-type, plus the
   precision×style resolution rule. Write it into `docs/` (a `format-model.md` or a
   subsystem-invariants section) before touching code.
2. [ ] **The code** — refactor `FormatAnnotation` resolution to the single model;
   the FC popup lights/no-ops controls per incoming type off it.

Gate: A2 (visual redesign) and A4 (units) both build on this. Do it first.

### A2 — Visual / layout redesign + SegToggle unification — `[ ]` NOT DONE

The aesthetic half the 2026-06-24 split moved to v1.1 (architecture was v0.9/DONE).
Building block ready: the shared `SegToggle` component exists (`SegToggle.tsx:9`) and
is already used by Cast/Chart/ColorPicker/Frame/Sparkline nodes — the FC just still
renders its own inline `.solenoid-fc__seg`/`__segbtn` variant (`FormatControllerNode.tsx:389-406`,
`FormatControllerNode.css:76-95`) instead of routing through it. No visual redesign has landed.

- [ ] **Layout redesign** of the FC's places / sig-figs / units panel. **Read
  `DESIGN.md` before any pixel** (hard rules: no accent stripe, Quiet Accent, no
  faux-3D). Pairs with A1 — the redesigned panel renders the function model's
  control matrix.
- [ ] **SegToggle unification** — route the FC's places/sig-figs toggle (and any new
  toggles) through the shared `SegToggle` component so there's one segmented-button
  definition. Today the FC keeps its own inline variant (`.solenoid-fc__seg` /
  `__segbtn`); `SegToggle` (Get Column / Add Column read-as) is the full-width form
  with the same look. Unify onto one.

### A3 — FC movement-function pass (docked FCs) — `[~]` PARTIAL

Real coverage on **drag** (`repositionDockedTo` on `nodetranslated`, `Canvas.tsx:2700`),
**group move** (`moveGroupMembers` translates docked nodes, `groupPush.ts:83-86`,
excluded from the movable box `:119`), and **tidy/autofit** (reserves a docked footprint
via `hostFootprint`/`realHostSize` + re-snaps, `Canvas.tsx:1544`/`1680-1696`/`1877`).
Uneven: **push / expand / collapse** ride the generic `shiftBox` translate (`groupPush.ts:78-88`)
without the footprint-reservation + re-snap rigor the tidy path has.

- [ ] **Audit the remaining movement ops for docked-FC correctness** (author 2026-06-19).
  Push / expand / collapse especially — bring them to the rigor the standoff-cluster pass
  gave standoffs. Touch: `repositionDockedTo`, `dockedNodeStore`, `realHostSize` / footprint
  logic in `Canvas.tsx`. Ties into the bug-lane mis-dock fix (both are docking correctness).

### A4 — Units by dimensionality (the big one, own milestone) — `[ ]` NOT DONE

Unit is still a single scalar per socket/value (`FormatAnnotation.unit: string`,
`formatAnnotationStore.ts:406`; `unitFlow` resolves one string per output socket). A
vestigial `FrameColumn.unit?` slot exists (`frame.ts:37-38`) and is preserved through
subframe/group-by but **never written** (`buildFrame` ignores header units) — so the
per-column lock and per-element list units don't exist yet. Building the representation
is the first task.

The flagship vision (author 2026-06-25). Keyed to the dimensional ladder:

- **Matrix → unit-AGNOSTIC, always** (pure numbers stripped of headers/formatting).
- **List → units PER ELEMENT.** A list is conceptually a ROW (`[3 km, 5 mi, 2 km]`);
  each element carries its own unit, the same way the array-semantics model already
  carries per-cell `null` + `SolError` (`valueKinds.ts`). A unit becomes a per-cell
  tag, not a per-value one.
- **Frame → units PER COLUMN** (already the direction of "FCs on frame columns").
  A frame row is a list, so per-column frame units == the per-element units of any
  extracted row.
- **FCs assign units to STRING-LIST elements used as HEADER KEYS** (can be later in
  the sub-milestone). An FC puts a unit on a header string; building a frame from
  that header list LOCKS each column to its header's unit.

**Worked target UX:** build an Orders header list `[id (none), Item (none), Revenue
($0.00)]` (a string list where "Revenue" carries `$0.00` via an FC), feed it to
Build Frame / Add Column with revenue values `[5, 6, 7]` → forcibly formatted to
`[$5.00, $6.00, $7.00]`, and the per-column FC inside the resulting Frame popup is
LOCKED by the header list's unit.

**Design-first task:** define the **per-cell / per-column unit representation** before
building (a parallel unit array? a tagged cell? matrices opt out). Then:
- [ ] value model — a list element / header gains an optional unit tag; matrices opt out.
- [ ] `unitFlow` — propagate per-element / per-column units; passthrough/transform
  rules apply per cell; Build Frame / Add Column pull the unit from the header.
- [ ] FC + display — render mixed unit suffixes; the FC popup edits a list/column's
  units; an FC can target a string-list header key.
- [ ] aggregators — SUM over mixed units → convert or `#TYPE!` (like element-family
  separation already does).
- [ ] socket lattice — units as the finer-grained sibling of the element-family
  separation it already enforces.

Independent of future dimensionality math (mph); carrying mixed units through
selection / filter / reshape / frame-build is valuable on its own. **Big — its own
milestone; design the representation first.**

**WS-A exit criteria:** one FC model resolves all four axes per value-type off a
written truth table; the FC panel is redesigned on `SegToggle`; docked FCs survive
every movement op; a unit rides per-element through a list and per-column through a
frame, and the header-list→locked-column worked example loads in a seed.

---

## WS-B — Packs & extensibility

The pack *framework* + the Geometry worked example are done, **and the persistence
gate is already built** (B1 below), so real packs can ship now. Design lives in
`pack-architecture.md`.

### B1 — Dormant-pack persistence — `[x]` DONE (gate satisfied)

Built already (verified 2026-07-01, contra the 2026-06-21 gap note). `rebuildGraph`
turns an unknown node `type` into a `PlaceholderNode` and synthesizes its sockets from
the saved connections via `deriveMissingNodeSockets` (`persistence.ts:345-371`,
`persistenceCore.ts:59+`), so **cables re-link — nothing is dropped**; the user is told
("kept as placeholders so your wiring and data are intact", `:236-239`). Round-trip is
lossless — `serializeGraph` re-emits a placeholder as its original `missingType` +
`savedInit`/`savedLiterals` (`:126-139`). Provenance is recorded as `SavedGraph.packs?:
string[]` (the active pack-id set, `:80-86`/`:184-186`) — a stored breadcrumb, **not yet
consumed on load** to auto-activate dormant packs.

- [ ] **Remaining (optional):** consume `SavedGraph.packs` on load to prompt/auto-activate
  the packs a save needs, and record pack *versions* (not just ids). Not a blocker for
  shipping a code pack — the no-cable-loss safety is already in place.

### B2 — New core I/O / visual / control nodes — `[~]` MOSTLY NOT DONE

Verified 2026-07-01. Existing Visuals are Sparkline / Chart (column·bar·line·area only,
`visual.ts:49`) / Chart Builder / Gauge / Heatmap. **Partial overlaps:** a Frame grid
*editor* exists (`FrameInputNode`, click-chip-to-edit) but not a read-only grid *viewer*
of an arbitrary upstream frame; multi-select exists only as `SlicerNode.multiSelect`, not
a standalone control. **Missing (net-new):** KPI/stat card w/ delta, standalone Dropdown,
Scatter, Bubble, Histogram, Bullet graph, dual-handle range slider, date-range picker.

- [ ] Build the core gaps from `io-visual-control-node-proposal.md` (KPI card, Dropdown,
  Multi-select, Data Table viewer, Scatter/Bubble/Histogram, Bullet, range slider,
  date-range). Specialist charts (box / candlestick / sankey / choropleth / 3-D) → packs,
  most behind a single optional heavy renderer (ECharts / Plotly). The first *code* pack
  (recharts-native: Funnel / Treemap / Sunburst / Sankey / Radar) proves the custom-widget
  pack path (B1 is done, so it's unblocked).

### B3 — Excel Timesavers pack — `[~]` PARTIAL (shell ships; idioms missing)

The pack is registered and active (`packs.ts:165-174`, id `"timesavers"`, `defaultActive`)
but it currently only **re-tags existing core nodes** into the pack (`NODE_PACK_TAGS`,
`:186-204`: rolling aggregates, weighted stats, argmax/argmin, list ops, XNOR/NAND/NOR)
plus one new node, `HYPOTENUSE_ENTRY`. **None of the proposed idioms** (Spell Number,
Split Name, Pareto, Join-If, rank-in-group, last-non-blank, duration trio…) exist as nodes.

- [ ] Build the proposed idioms from `timesavers-pack-proposal.md`. ~35 "no single Excel
  function" idioms:
  ~25 cheap **formula-data** pre-set Expression nodes (one JSON file, first real
  exercise of the formula-pack path beyond Geometry), ~8 **custom-logic** (Spell
  Number, Reverse, duration trio, Split Name, list-reducers, multi-criteria lookup,
  last-non-blank, rank-in-group), ~3 **composites** (Pareto, Join-If, Lookup-all).
  Several list-reducers are general enough to build as **core** Lookup/Aggregation
  nodes and merely tag into Timesavers. The `[C]` custom-logic + composites depend on
  B1; the `[F]` formula-data presets do not.

### B4 — Additional domain packs

- [ ] Electromagnetics (from the flux seed), Finance-pro, etc. Post-v1 polish; the
  capability is proven by Geometry. Each code pack depends on B1.

### B5 — Pack infrastructure follow-ups

- [ ] **Variant switch reconciles the socket set** — a simple pack's variant dropdown
  switches formulas with different variables; switching must add/remove sockets and
  preserve cables on shared variables. Reuse the Expression node's socket
  re-derivation, not new machinery.
- [ ] **Aliasing — many internal ports → one shell parameter** (the stats "confidence
  level" case). Decide v1.1 vs fast-follow: clean vs cluttered advanced panel.
- [ ] **Error UX on restriction violation** — typed error out the socket vs the node
  flagging the offending input locally. Pending.

**WS-B exit criteria:** deactivating a pack placeholders its nodes (no cable loss) and
records provenance in the save; at least one code pack ships on that foundation; the
Timesavers formula-data presets load.

---

## WS-C — Canvas & interaction

Independent polish. Each item is self-contained; pick up in parallel with WS-A.

### C1 — Grid system — `[ ]` NOT DONE

Only two narrower things exist (neither is this item): snap-on-release (`gridSnapStore`,
`snapCoord`, `Canvas.tsx:2737`) and Ctrl-drag node-edge alignment to the last-grabbed node
(`snapAxis`, `ALIGN=8`, `:2667-2696`). No alignment guides, no primary+sub-grid, no
drag-time bypass modifier.

- [ ] Implement `docs/grid-system.md` (headed FUTURE/unimplemented): soft grid alignment
  (not forced snap) across node placement, alignment helpers/guides, note/group corners;
  primary grid + sub-grid + modifier-to-bypass. NOTE: cable shapes shipped grid-free (the
  walk router keys off socket positions), so gridding cables is a separate design question
  (see the doc's banner) — don't fold it in.

### C2 — Cable collision avoidance

Deferred from the routing spec (`cable-routing.md` §2). The shape system (walk router
+ tangent-exact spline) shipped; these are the routing-quality extensions:
- [ ] **Avoid nodes** — cables route around node bounding boxes.
- [ ] **Avoid cables** — parallel runs + bridge hops at crossings.
- [ ] **Per-cable shape / avoidance overrides** (graph-wide today).

**Invariant guard:** anything here touches `cablePaths.ts` — LENGTH stays the primary
sort key of route selection (gating on turn count first causes visible jumps), and
`cablePaths.test.ts` continuity must stay green. See subsystem-invariants "Cable
routing". Also honor `feedback-never-degrade-cables-in-motion` — full curved fidelity
always, no straightening during drag/pan/zoom.

### C3 — Moveable / resizable / hideable UI chrome — `[~]` PARTIAL

Already done: **hiding individual chrome** — each panel registers its own open/close via
`registerChrome` (navigator/pins/alerts/legend), Tab toggles all as a group
(`chromeToggle.ts:22`), and the minimap has a global hide (`perfHideMinimap`,
`settingsStore.ts:22`). Still missing:

- [ ] **Minimap position** — DECIDED 2026-07-01: NOT four corners. One **3-way setting**
  (a dropdown that absorbs today's Hide-minimap toggle): **Bottom · Top · Hide**.
  - **Bottom** (current default): minimap bottom-right. When set to **Hide**, the **Socket
    Reference (SR)** legend slides down to fill the freed space (this should already happen on the
    current hide path — verify/fix).
  - **Top**: minimap moves to top-right, sitting **below the existing Zoom & More pill**; SR
    slides down accordingly.
  - Implementation: a position field on `settingsStore` (replacing the bare `perfHideMinimap`
    boolean with a `Bottom|Top|Hide` enum), a position prop on `SolenoidMinimap`, and the SR/legend
    layout keys off it.
- **Resize panels** — DEFERRED to **1.2** (2026-07-01). Not per-panel drag handles; instead
  folded into a future whole-app **UI-scale pass**: one **Default / Larger** toggle that scales the
  entire app UI. This resize bullet is subsumed by that toggle; don't build per-panel resize in 1.1.
- [ ] **Palette override editor UI** — a surface to edit the open doc's per-slot overrides
  (and maybe author/save custom named palettes) from inside the app. The store + persistence
  already support it (`SavedGraph.palette`, `palette.ts:324/336`); Settings only switches the
  app-wide base with a `readOnly` swatch grid. This is just the missing editor UI.

### C4 — Pinch-zoom on trackpad — `[x]` DONE

`CappedZoom extends Zoom` overrides `wheel` and is installed via `area.area.setZoomHandler`
(`Canvas.tsx:123`/`:1173`); comments confirm it targets "trackpad pinch" (macOS pinch arrives
as a `ctrlKey` wheel event, which this path handles). Nothing to do; the author should still
confirm the feel on real Mac hardware.

### C5 — Small polish — `[ ]` NOT DONE

Verified 2026-07-01: the value popups expose only Pin + Close (plus one bare Copy/Copy-CSV in
`TablePopup`); the five pure-visual nodes are all `collapsible={false}` so they never collapse.

- [ ] **Pop-up "+ more" actions** — DECIDED 2026-07-01: add a **3-dot (`⋯`) overflow menu** to
  the popup header holding **Go to node** (`flyToNode(hostId)` — already exists at `flyToNode.ts:14`,
  wire to the popup's `useHostNodeId`), **Export CSV** (file save), and **Copy as Markdown**. Also
  **move the existing Copy-CSV** into that menu. Table/Frame/List popups get the CSV/Markdown items;
  Go-to-node applies to all popups.
- [ ] **Collapsed mini-preview for pure-visual nodes** — Chart / Gauge / Slicer / Sparkline /
  Heatmap are `collapsible={false}` today (collapsing to "—" was pointless). A thumbnail or
  passed-through value would let them collapse meaningfully.

---

## WS-D — Data & integration

Node-level integrations, mostly desktop (need the Tauri fs / http layer).

- [ ] **Obsidian markdown import/export — bidirectional sync** (author 2026-06-23).
  Builds on the frontmatter→sockets work. (1) Import a `.md` file as a Note node so
  its YAML keys become typed output sockets; ideally watch/re-read so editing it in
  Obsidian flows into the graph. (2) A markdown EXPORT node — write a Note (or computed
  record) back out as `.md` with frontmatter. Needs a file-path / vault binding + the
  Tauri fs layer (`fileBridge`); the parse (`noteFrontmatter.ts`) + serialize already
  exist for the in-graph half. North-star: a vault of notes ↔ a frame of records.
- [ ] **Image bundling** — embed locally-attached images so they survive save/reload.
  Pure-JSON saves can't carry image bytes without bloat, so this needs a bundle format
  (a `.zip`/folder of graph JSON + assets, or a sidecar) with the local `dataUrl`
  plumbed in. Until then a local attach shows "not saved"; a URL persists.
- [ ] **Finance connection** (GOOGLEFINANCE-ish) — a thin Web Source preset over a
  chosen finance API (current quote + historical table). Brings API-key + rate-limit
  baggage; do only if wanted. Google's backend isn't reusable.
- [x] **Multi-level Nest Join — DONE** (verified 2026-07-01). `NestJoinNode` (`nodes/cube.ts:88`)
  has an `any` Parent socket that accepts a **Cube** to deepen the hierarchy one level
  (Customer→Order→LineItem, dispatch at `:112-121`; verb `nestFrame`/`unnestCube` in
  `frameVerbs.ts:678/710`). Nothing to do here.
- [ ] **Unified XLOOKUP — one node for list + frame + cube** (author decision 2026-07-01).
  Merge the three lookups into ONE node, keep the name **XLOOKUP**, "just be able to handle
  everything." Design notes (not yet built):

  **What it subsumes** (both already shipped in v1.0, to be folded in — not greenfield):
  - the **list** XLOOKUP with match/search modes (`XLookupNode`, `lookup.ts:26`);
  - the **frame** XLOOKUP (`FrameLookupNode` → `lookupFrameCell`, exact-match, scalar `any`,
    `frame.ts:1007`).
  Plus the net-new **cube** case (look a key up in a Cube). One node replaces both classes.

  **Core behavior — return the matched value WHOLE, no drill-down** (author): XLOOKUP does NOT
  navigate into nested structure. It finds the matched position and returns whatever value sits
  there, as-is. That value can be:
  - a scalar → scalar out;
  - a 1-D list (e.g. the matched row across several return columns) → list out;
  - a **2-D value** (a whole nested frame/matrix in the return cell, or the matched row as a
    single-row frame) → frame/matrix out;
  - a **2-D value whose cells themselves nest** → **XLOOKUP outputs a CUBE** (author's explicit
    constraint: the node must be able to emit a cube). Drilling into that cube is the downstream
    CubePopup / positional INDEX's job, not XLOOKUP's.

  **Output socket = `any`** (already cube-capable in the lattice). The concrete result type is
  only known at compute, and the socket is static, so `any` carries whichever of scalar / list /
  frame / cube comes out — the same reason today's Frame Lookup uses `any`. Cube-vs-frame is a
  runtime check on the pulled value: it's a cube iff the returned 2-D value has ≥1 nested cell
  (`isCubeValue`), else a flat frame/matrix.

  **Match/search modes** carry across all source types where ordered keys make sense
  (exact / next-smaller / next-larger; first-last / last-first / binary). Exact only for
  text/logical keys. Reuse the frame path's type-aware key match (string / numeric /
  date-serial-or-ISO / logical); a null/error key never matches (join-key rule).

  **Open design questions to settle before coding:**
  1. **Input surface** — one node must accept a two-list pair (keys + values) AND a
     container-plus-column-name form (frame/cube + In column / Return). Lean toward **duck-typing
     on the wired source**: an `any` source socket, show list-pair sockets when a bare list is
     wired vs In/Return column-name inputs when a frame/cube is wired (consistent with the
     polyform / `any` nodes). Alternative: an explicit source-mode selector. Decide.
  2. **What "return" means for a frame/cube** — one Return column (→ that cell), OR multiple
     columns / the whole row (→ the "2-D value" case). Decide whether Return takes one name, a
     list of names, or `*` for the whole row.
  3. **Migration** — one class replaces `XLookupNode` + `FrameLookupNode`; pre-alpha, so break
     both, fold `frame-lookup` into the single `xlookup` catalog entry, update seeds/tests, no
     compat shim.
  4. **Approximate match + typed read-as** — the two follow-ups already named on Frame Lookup
     (`frame.ts:1003`) fold into this unified node.

  See `cube-node-scope.md`. Unchanged: flat verbs still do NOT map over nested cells
  (Unnest → verb → Nest explicitly) — XLOOKUP returning a cube whole is not "mapping over cells."

---

## WS-E — Engine scale niceties (opportunistic)

Not blockers; the eager Rust engine + JS CSV parse are the current ceiling. Pull in
if heavy-table perf becomes a real complaint.

- [ ] **Lazy-plan fusion** — the lazy-handle finish (2026-06-30) keeps a verb chain in
  the backend but each verb still materializes a full DataFrame in Rust. True lazy
  `LazyFrame` plan fusion (compose the whole chain, collect once) is the next win.
- [ ] **Direct CSV→Polars reader** — skip the JS CSV parse for desktop imports; read
  straight into Polars.
- [~] **Formula engine re-audit + native-math deletion** — the **audit is DONE** (full sweep
  2026-06-25, `excelFunctions.ts:442`; per-family `FAMILY_BACKING` verdicts at `:85-98`,
  `formulajs-vs-native-audit.md`). What's **NOT done** is the coordinated deletion: routing the
  `formulajs`-backed families' NODES through the seam and removing the redundant hand-rolled math
  (`excelFunctions.ts:26-29` says so explicitly) — no correctness impact, pure single-source
  hygiene. Also standing follow-up: re-sweep when nodes change or Formula.js upgrades (new
  divergences like the MOD/ATAN2 FX bugs appear); widen `distributionFormula.test.ts` ranges if
  accuracy is ever in question.

---

## Cross-cutting

- **Read `DESIGN.md` before any UI/visual change** (WS-A redesign, WS-C chrome, new
  visual nodes). The Quiet Accent Rule and the no-accent-stripe rule are the ones you
  violate blind.
- **Subsystem invariants gate the risky touches** — WS-C cable work against
  `cable-routing`, WS-A movement pass against `standoffs` / group-push / tidy. Verify
  against `subsystem-invariants.md`, don't eyeball.
- **`tsc` + `vitest` green after every step; verify visuals on the running app** (the
  author eyeballs; no puppeteer). Web/desktop split stays load-bearing — protect the
  `FrameBackend` seam.
- **Pre-alpha still applies** — break old saves freely; no back-compat shims for
  renames/format changes (the units-representation change in A4 will touch the save
  format; just update seeds + tests).

## Suggested milestones

1. **v1.1-α — FC model + redesign.** WS-A1 (function model spec + code) + A2 (visual
   redesign / SegToggle) + A3 (finish the remaining movement ops). The lone bug-lane
   item (FC mis-dock) lands alongside — it's the same docking surface as A3.
2. **v1.1-β — units.** WS-A4 (units by dimensionality) as its own milestone, plus the
   first code pack (B1 is already built, so it's unblocked).
3. **v1.1 ship.** WS-C polish + WS-D integrations that made the cut; WS-E as pulled in.

WS-A is the release's reason to exist; B/C/D/E are the tail that rides along.

**Reality check (grounded 2026-07-01):** what's *already done* and needs no work —
Bug 1 (navigator rows), B1 (dormant-pack persistence), C4 (pinch-zoom), the
multi-level Nest Join, and the formula re-audit. What's *partially* there — A3
(docked-FC movement, drag/group/tidy covered), C3 (per-panel hide done), B2/B3
(building blocks + pack shell, no net-new nodes/idioms), WS-E formula (audit done,
deletion pending). Everything else is genuinely greenfield.

## Open decisions to confirm (author review — not blocking the plan)

1. **Units representation (A4)** — parallel unit array vs tagged cell? Design-first,
   author call. Drives the save-format change.
2. **Aliasing (B5) — v1.1 or fast-follow?** Clean vs cluttered advanced pack panel.
3. **Finance connection (WS-D)** — build at all? API-key/rate-limit baggage; only if
   wanted.
4. **Which WS-C/WS-D items make the 1.1 cut vs slip to 1.2** — the tail is long;
   author prioritizes.
