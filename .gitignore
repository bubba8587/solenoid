# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local state (keep .claude/skills tracked)
.claude/settings.local.json
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Affinity Designer editor lock files
*~lock~
sample-data/

# generated copy-rewrite working files (scripts/copy-inventory.ts)
copy-inventory.md
copy-inventory.md.orig.json
/rf-*.png

# Dev-server mirror of the document being edited (vite.config.ts devGraphMirror)
.dev/

# The Obsidian plugin's local build (`npm run plugin:build`)
obsidian-plugin/dist/

# Plugins installed locally into the demo vault from the community store (TaskNotes, Solenoid
# Properties); not ours to ship
demo-vault/.obsidian/plugins/
# Obsidian writes these when the demo vault is opened; only daily-notes.json, types.json, community-plugins.json,
# appearance.json (it turns the Solenoid snippet on) and snippets/ are fixture
demo-vault/.obsidian/workspace.json
demo-vault/.obsidian/app.json
demo-vault/.obsidian/core-plugins.json
demo-vault/.obsidian/graph.json
# TaskNotes generates its view bases here
demo-vault/TaskNotes/
# The decision tree opened as an Obsidian vault: personal workspace state.
tree/.obsidian/
tree/decisions/.obsidian/
__pycache__/
